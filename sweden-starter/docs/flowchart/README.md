# Workflow flowchart

Drop the visual workflow flowchart here (being produced in Claude Design).
It is the visual companion to `ACTIVATE.md` and `docs/OPERATING-MODEL.md` —
shown to new operators and clients during install.

Expected: a single SVG/PNG (plus source file) showing the loop:
CD → intake → CC canon → print to Figma → Figma-first work → read-back → canon.
