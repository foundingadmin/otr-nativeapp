# cases/system-case_status_and_action — Ledger

**Workstream:** Case status badge registry + CPC per-state system. PRD §6–§9.

## Canonical (current)

- `Case Status and Action.html` — the approved badge registry: form, weight, icon, action pairings. Source of truth for status language. (DR-X-001/003/004/006)

> **Moved out (Jul 7):** `Case Preview Cards.html` (CPC per-state specs, PRD §9) now lives in its own workstream → `cases/component-case_preview_cards/`. This workstream keeps the badge/status *language*; the card *component* is tracked there.

## Explorations

- `explorations/01-status-series/` — Series 1–3 badge treatment exploration. **Status: decided — integrated** via DR-X-001/003/006 into the Case Status and Action board. Candidate to archive on next pass.

## Decisions

No workstream-local DRs yet (all backfilled decisions are cross-cutting → `cases/_docs-cases/decisions/`). Next local DR: **DR-SS-001** — gap-state badges (Counter offer, Remarketing, Cancelled), see ROADMAP Next #1.

## Known gaps

- Counter offer / Remarketing / Cancelled have **no approved badge** (PRD §6.2). Highest-priority gap before build.
