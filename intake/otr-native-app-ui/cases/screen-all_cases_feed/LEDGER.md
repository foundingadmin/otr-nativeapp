# cases/screen-all_cases_feed — Ledger

**Workstream:** Cases tab feed of Case Preview Cards. PRD §8, §11.

## Canonical (current)

- `All Cases Feed.html` — assembled feed proposal (kits: `feed-kit.jsx`, `feed-assembled.jsx` + _ds-cases kits).

## Explorations

- `explorations/01-feed-exploration/` — layouts, filters, action treatments. **Status: exploring.**

> **Moved out (Jul 7):** the header-divergences exploration was a CPC *card* identity-zone change, not a feed-page change — it moved to `cases/component-case_preview_cards/explorations/01-header-divergences/`.

## Archive

- `archive/00-early-divergence/` — original mega-canvas (Boards B–F), pre-system. Superseded by the workstream split; winning directions flowed into Case Feed + Case Details canonicals. Self-contained (kits copied in).
- `archive/01-prototype/` — first interactive feed prototype. Superseded by `All Cases Feed.html`. Self-contained.
- `archive/02-divergence-pass-1/` — Cases tab divergence pass 1 (Boards A–F). ⚠️ **Source-only:** depends on `otr-kit.jsx`/`otr-cpc.jsx`, deleted before this archive system existed — board no longer renders. Kept for record.
- `archive/03-variants-a-d/` — Cases tab variants A–D. ⚠️ **Source-only:** same deleted-kit dependency (`otr-kit`, `otr-cpc`, `otr-f-kit`).

## Decisions

No workstream-local DRs yet. Numbering: **DR-CF-###**. First expected: a feed exploration verdict (exploration 01, exploring).
