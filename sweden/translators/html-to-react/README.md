# Translator: html-to-react

Translates canonical HTML/CSS components into React components (.tsx files).

Output lands in `brand/components/react/`.

**Status:** Scaffolded — not yet implemented

This translator runs on-demand, not on every sync.
